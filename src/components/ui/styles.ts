// Shared class-name constants for Factura's design-system primitives —
// square corners, mono uppercase labels, hairline borders, one accent.

/** The recurring tiny mono uppercase caption used for field/section labels. */
export const microLabel =
  "font-mono text-[10px] uppercase tracking-[0.14em] text-muted";

/** Muted helper / explanatory text used throughout forms and the builder. */
export const hint = "font-mono text-[11.5px] text-muted leading-[1.6]";

/** Shared input/select chrome — mono, square, hairline border, accent focus. */
export const FIELD_BASE =
  "font-mono text-sm text-ink bg-paper border border-line rounded-none py-2 px-3 outline-none transition-colors box-border focus:border-accent";
